<!DOCTYPE html>
<html>
  <head>
    <title>Base Rails</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>


 <link rel="stylesheet" href="/css/my_styles.css">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js"></script>
    
  </head>

<body>
    <ul>

    <a 
    href="/stores">Stores</a></
    </ul>

    <ul>


      <% if @current_user == nil %>
        <li> <a class="btn btn-outline-success"
        href="/user_sign_in">Sign In</a></li>
        <li> <a class="btn btn-outline-success"
         href="/user_sign_up">Sign Up</a></li>
      <% else %>
        <a class="btn btn-outline-danger"
        
        href="/user_sign_out">Sign Out</a>
      <% end %>
    </ul>


  <div style="color: purple"><%= notice %></div>


    <%= yield %>
  </body>
</html>
